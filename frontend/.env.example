VITE_BACKEND_ENDPOINT=
VITE_API_TIMEOUT_MS=
# OAuth client ID from Google Cloud Console (APIs & Services > Credentials).
# Must match GOOGLE_CLIENT_ID on the backend. The "Sign in with Google"
# button on the login page stays hidden until this is set.
VITE_GOOGLE_CLIENT_ID=
